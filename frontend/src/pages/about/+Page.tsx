import AboutMenu from "../../components/AboutMenu";

const Page = () => {
    return (
        <main className="flex flex-row place-content-evenly items-start px-3">
            <AboutMenu />
        </main>
    );
};

export default Page;
